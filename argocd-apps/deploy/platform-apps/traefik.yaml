---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
spec:
  project: platform-apps
  source:
    repoURL: https://traefik.github.io/charts
    targetRevision: 39.0.2
    chart: traefik
    helm:
      values: |
        # Schedule on worker node with ingress port mappings
        nodeSelector:
          ingress-ready: "true"
        # Use Kind's port-mapping
        service:
          type: NodePort
        # Use hostPort to bind directly to Kind's mapped ports
        ports:
          web:
            hostPort: 80
          websecure:
            hostPort: 443
        # Single replica for local dev
        deployment:
          replicas: 1
        # Enable dashboard for learning/debugging
        ingressRoute:
          dashboard:
            enabled: true
            matchRule: Host(`traefik.lab.internal`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
            entryPoints:
              - websecure
            tls:
              certResolver: stepca
        # ACME certificate resolver using step-ca as private CA
        certificatesResolvers:
          stepca:
            acme:
              caServer: https://step-ca-step-certificates.traefik.svc.cluster.local/acme/acme/directory
              storage: /data/acme.json
              httpChallenge:
                entryPoint: web
        # Persist ACME state across restarts
        persistence:
          enabled: true
          size: 128Mi
          path: /data
        # Trust step-ca root CA (chart-generated ConfigMap, same namespace)
        volumes:
          - name: step-ca-step-certificates-certs
            mountPath: /step-ca-certs
            type: configMap
        env:
          - name: LEGO_CA_CERTIFICATES
            value: /step-ca-certs/root_ca.crt
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
